# Weather Briefing MCP currently requires no API keys or secrets.
# Open-Meteo endpoints (geocoding + forecast) require no authentication.
# If a future integration needs a key, add it here as a placeholder, e.g.:
# SOME_API_KEY=your-key-here