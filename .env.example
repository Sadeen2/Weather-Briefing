# This project currently uses no environment variables.
# Open-Meteo endpoints (geocoding + forecast) require no API key.
# If a future integration needs a key, add it here as a placeholder, e.g.:
# SOME_API_KEY=your-key-here